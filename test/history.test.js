import { expect } from 'chai'
import sinon from 'sinon'
import fs from 'node:fs'
import path from 'node:path'
import {
    loadHistoryFromFile,
    getHistory,
    addToHistory,
    removeFromHistory,
    setHistory,
    saveCurrentHistoryToFile
  } from '../lib/historyManager.js'


describe('startScanner history file testing', function () {


  beforeEach(async () => {
    
  })

  afterEach(async () => {
 
  })

  it('write 5 items and read from file', async function () {

    
   
  })
})
